import spaces from "./spaces";

const grid = {
  mobile: {
    columns: 4,
    margin: spaces["16px"],
    gap: spaces["16px"],
  },
  desktop: {
    columns: 12,
    margin: spaces["56px"],
    gap: spaces["24px"],
    container: "1366px",
  },
};

export default grid;
