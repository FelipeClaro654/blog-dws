import spaces from "./spaces";

const borders = {
  radius: {
    circle: "50%",
    small: spaces["8px"],
    regular: spaces["16px"],
    upperOnly: `${spaces["16px"]} ${spaces["16px"]} 0 0`,
  },
};

export default borders;
