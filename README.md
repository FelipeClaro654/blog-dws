# DWS Blog

This React + Vite + TanStack Query application serves blog posts with details page.
Run the following commands to run the application:

- pnpm install
- pnpm dev

## Tests

To run tests:

- pnpm run test

